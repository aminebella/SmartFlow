<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.2.5</version>
		<relativePath/>
	</parent>
	<groupId>emsi</groupId>
	<artifactId>SmartFlow</artifactId>
	<version>0.0.1-SNAPSHOT</version>

	<properties>
		<java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
            <sonar.coverage.exclusions>
                **/entity/**,
                **/rmi/**,
                **/soap/**,
                **/configuration/**,
                **/dto/**,
                **/exception/**,
                **/repo/**,
                **/keys/**,
                **/enums/**,
                **/email/**,
                **/role/**,
                **/SmartFlowApplication.*
            </sonar.coverage.exclusions>
	</properties>

	<dependencies>

		<!-- Web -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
			<!-- spring-boot-starter-web → makes it a REST API server (like Express/Laravel routes) -->
		</dependency>

		<!-- JPA + MySQL -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
			<!-- spring-boot-starter-data-jpa → ORM, lets you talk to DB using Java objects (like Eloquent) -->
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<!-- mysql-connector-j → MySQL driver -->
			<scope>runtime</scope>
		</dependency>

		<!-- Security -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
			<!-- spring-boot-starter-security → authentication & authorization system -->
		</dependency>

		<!-- Mail -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
			<!-- spring-boot-starter-mail → sending emails -->
		</dependency>

		<!-- Thymeleaf -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
			<!-- spring-boot-starter-thymeleaf → HTML email templates (like Blade but for emails) -->
		</dependency>
		<dependency>
			<groupId>org.thymeleaf.extras</groupId>
			<artifactId>thymeleaf-extras-springsecurity6</artifactId>
		</dependency>

		<!-- Swagger -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<!-- springdoc-openapi → generates Swagger UI at /swagger-ui.html automatically -->
			<version>2.3.0</version>
		</dependency>

		<!-- Lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<!-- lombok → removes boilerplate: generates getters/setters/constructors via annotations -->
			<optional>true</optional>
		</dependency>

		<!-- JWT -->
		<!-- jjwt → JWT library for creating and validating tokens -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.11.5</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.11.5</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.11.5</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Test -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>

        <!-- Resilience4J -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
            <version>2.2.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- Ajoute à la place : -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.1</version>
        </dependency>

        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.2.5</version>
        </dependency>
        <!-- AMQP / RabbitMQ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>

        <!-- Spring Context -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context-support</artifactId>
            <version>6.1.6</version>
        </dependency>

        <!-- Spring Remoting -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-remoting</artifactId>
            <version>2.0.8</version>
        </dependency>

        <!-- Kotlin -->
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib-jdk8</artifactId>
            <version>${kotlin.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-test</artifactId>
            <version>${kotlin.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Spring Web Services (SOAP) -->
        <dependency>
            <groupId>org.springframework.ws</groupId>
            <artifactId>spring-ws-core</artifactId>
        </dependency>

        <!-- JAXB Runtime -->
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
        </dependency>

        <!-- WSDL4J -->
        <dependency>
            <groupId>wsdl4j</groupId>
            <artifactId>wsdl4j</artifactId>
        </dependency>

        <dependency>
            <groupId>me.paulschwarz</groupId>
            <artifactId>spring-dotenv</artifactId>
            <version>4.0.0</version>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
<!--            <scope>test</scope>-->
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

	</dependencies>

	<build>
		<plugins>
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>3.11.0.3922</version>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.11</version>

                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>

                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>

                </executions>

                <configuration>
                    <outputDirectory>${project.build.directory}/jacoco-report</outputDirectory>
                    <excludes>
                        <!-- Spring configuration classes -->
                        <exclude>emsi/SmartFlow/configuration/**</exclude>

                        <!-- Converter classes if any -->
                        <exclude>emsi/SmartFlow/controller/converter/**</exclude>

                        <!-- DTOs (pure data carriers) -->
                        <exclude>emsi/SmartFlow/controller/dto/client/**</exclude>
                        <exclude>emsi/SmartFlow/controller/dto/projectMember/**</exclude>
                        <exclude>emsi/SmartFlow/controller/dto/sprint/**</exclude>
                        <exclude>emsi/SmartFlow/controller/dto/teams/**</exclude>
                        <exclude>emsi/SmartFlow/controller/dto/AdminDashboardSummary*</exclude>
                        <exclude>emsi/SmartFlow/controller/dto/AiAnalysisRequest*</exclude>
                        <exclude>emsi/SmartFlow/controller/dto/AiAnalysisResponse*</exclude>
                        <exclude>emsi/SmartFlow/controller/dto/ApiResponse*</exclude>
                        <exclude>emsi/SmartFlow/controller/dto/ClientDashboardSummary*</exclude>

                        <!-- Email (hard to test without SMTP) -->
                        <exclude>emsi/SmartFlow/email/EmailTemplateName*</exclude>

                        <!-- Entities (JPA classes, no logic to test) -->
                        <exclude>emsi/SmartFlow/entity/**</exclude>

                        <!-- Enums -->
                        <exclude>emsi/SmartFlow/entity/enums/**</exclude>

                        <!-- Entity keys -->
                        <exclude>emsi/SmartFlow/entity/keys/**</exclude>

                        <!-- Exception classes (no logic) -->
                        <exclude>emsi/SmartFlow/exception/**</exclude>

                        <!-- Handlers generated code -->
                        <exclude>emsi/SmartFlow/handller/BusinessErrorCodes*</exclude>
                        <exclude>emsi/SmartFlow/handller/ExceptionResponse*</exclude>

                        <!-- JPA Repositories (Spring-generated, no code to test) -->
                        <exclude>emsi/SmartFlow/repo/**</exclude>

                        <!-- RMI generated code -->
                        <exclude>emsi/SmartFlow/rmi/**</exclude>

                        <!-- SOAP generated code -->
                        <exclude>emsi/SmartFlow/soap/**</exclude>

                        <!-- Role entity -->
                        <exclude>emsi/SmartFlow/role/**</exclude>

                        <!-- RabbitMQ consumer/producer (integration concerns) -->
                        <exclude>emsi/SmartFlow/service/NotificationConsumer*</exclude>
                        <exclude>emsi/SmartFlow/service/NotificationProducer*</exclude>

                        <!-- Security config and filters -->
<!--                        <exclude>emsi/SmartFlow/security/SecurityConfig*</exclude>-->
                        <exclude>emsi/SmartFlow/security/LogoutService*</exclude>
                        <exclude>emsi/SmartFlow/security/UserDetailsServiceImpl*</exclude>

                        <!-- User entity and token -->
                        <exclude>emsi/SmartFlow/user/User*</exclude>
                        <exclude>emsi/SmartFlow/user/Token*</exclude>

                        <!-- Utilities (simple static helpers) -->
                        <exclude>emsi/SmartFlow/Utils/SecurityUtils*</exclude>
                        <exclude>emsi/SmartFlow/Utils/ClientUtils*</exclude>

                        <!-- Application entry point -->
                        <exclude>emsi/SmartFlow/SmartFlowApplication*</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.jvnet.jaxb</groupId>
                <artifactId>jaxb-maven-plugin</artifactId>
                <version>4.0.0</version>
                <executions>
                    <execution>
                        <id>generate</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <schemaDirectory>${project.basedir}/src/main/resources</schemaDirectory>
                    <schemaIncludes>
                        <include>project.xsd</include>
                    </schemaIncludes>
                    <generateDirectory>${project.build.directory}/generated-sources/jaxb</generateDirectory>
                    <generatePackage>emsi.SmartFlow.soap.generated</generatePackage>
                    <verbose>true</verbose>
                </configuration>
            </plugin>

			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<jvmArguments>--enable-preview</jvmArguments>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.2</version>
                <configuration>
                    <argLine>@{argLine} --enable-preview</argLine>
                </configuration>
            </plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
				<configuration>
					<source>21</source>
					<target>21</target>
                    <release>21</release>
					<compilerArgs>
						<arg>--enable-preview</arg>
					</compilerArgs>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>1.18.32</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

            <!-- Kotlin -->



		</plugins>
	</build>

</project>