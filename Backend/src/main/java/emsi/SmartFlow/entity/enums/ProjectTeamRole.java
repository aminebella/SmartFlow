package emsi.SmartFlow.entity.enums;

public enum ProjectTeamRole {
    MEMBER, // → Can view, update their tasks
    MANAGER // → Can manage the project and its team members, create tickets, manage sprints, etc.
}
